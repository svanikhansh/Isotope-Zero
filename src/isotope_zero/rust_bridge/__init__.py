"""Rust source for the isotope_zero._native PyO3 extension.

This package directory holds the .rs sources compiled by maturin into
``isotope_zero._native``. It is NOT importable as Python; the compiled
extension lands at the package root as ``isotope_zero/_native*.so``.
"""
